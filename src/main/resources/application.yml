spring:
  datasource:
    url: jdbc:mysql:///security?useUncode=true&characterEncoding=UTF-8
    username: root
    password: root
    type: com.zaxxer.hikari.HikariDataSource
    driver-class-name: com.mysql.jdbc.Driver

  redis:
    host: localhost
    port: 6379
    password:
    database: 1
mybatis:
  mapper-locations: classpath:mapper/*.xml
server:
  port: 8888
